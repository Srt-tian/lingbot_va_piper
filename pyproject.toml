[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lingbot-va-piper"
version = "0.1.0"
description = "Piper training and inference adaptation of LingBot-VA"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["wan_va*"]
